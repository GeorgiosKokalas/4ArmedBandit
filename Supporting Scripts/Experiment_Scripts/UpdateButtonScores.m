% Function called by: Experiment.m
% Role of function is to update the values of each button after every trial
% Inputs: 
%   - Button_Scores (The scores of all buttons in a list)
% Outputs: 
%   - Button_Scores (The scores of all buttons in a list)

function Button_Scores = UpdateButtonScores(Button_Scores)
    for button_idx = 1:length(Button_Scores)
        curr_score = Button_Scores(button_idx);
        % TO BE FILLED OUT

    end
end